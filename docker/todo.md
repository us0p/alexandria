- what happen when a container stops running? why does docker ps -a list 
  stopped containers? does stoped containers still hold memory?
- docker network
- docker volumes
- docker bind mount
- what is an image
- how to create an image
