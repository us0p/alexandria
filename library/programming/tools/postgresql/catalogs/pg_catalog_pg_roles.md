Describes information about database roles:
- `rolsuper`: If it's super user
- `rolcreaterolw`: If can create roles
- `rolcreatedb`: If can create databases
- `rolcanlogin`: If can log in (determines if it's a user and not a group or role)